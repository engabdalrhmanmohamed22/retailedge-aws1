{
    "name": "retailedge/app",
    "description": "RetailEdge Inc. - PHP e-commerce application (LAMP stack, migrated to AWS)",
    "type": "project",
    "require": {
        "php": ">=8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit --testsuite unit"
    }
}
